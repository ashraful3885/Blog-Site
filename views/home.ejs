<%- include("partials/header") -%>

<h1>Home</h1>
<p><%= home %></p>
<br>
<div class="content">
    <% blogPost.forEach((post)=>{ %>
        <h2><%= post.title %></h2>
        <p><%= post.body.substring(0, 200) + '...' %><a href="/posts/<%= post._id %>">ReadMore</a></p>
    <% }) %>
</div>

<%- include("partials/footer") -%>